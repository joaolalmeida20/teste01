function somar(a, b) {
    return a + b
}

const operacao = somar

console.log(operacao(4, 5))

const subtrair = function (a, b) { //função anonima, não precisa de um nome, após uma atribuição de variável.
    return a - b
}

console.log(subtrair(36, 13))

